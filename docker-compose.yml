version: '3.8'

services:
  alzheimers-app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: alzheimers-fastapi
    environment:
      - PORT=5000
    ports:
      - "5000:5000"
    restart: unless-stopped
